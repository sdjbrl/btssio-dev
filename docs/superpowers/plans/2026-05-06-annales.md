# Annales BTS SIO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter une section Annales complète à BTSSIO.DEV avec anciens sujets (2019–2024), questions reformulées et corrections indicatives pour toutes épreuves (E4, E5 SISR, E5 SLAM, E6, Tronc Commun).

**Architecture:** Données centralisées dans `lib/annales-data.ts` · Page liste `/annales` avec filtres client-side · Page détail `/annales/[id]` avec accordéons correction · Widget dans `/examens` · Lien Navbar.

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind CSS, lucide-react

---

## File Map

| Action | Fichier | Responsabilité |
|--------|---------|----------------|
| Create | `lib/annales-data.ts` | Types + toutes les données annales |
| Create | `components/AnnaleCard.tsx` | Card résumé d'une annale |
| Create | `components/AnnaleFilter.tsx` | Filtres client-side (année/épreuve/option) |
| Create | `app/annales/page.tsx` | Page liste filtrée |
| Create | `app/annales/[id]/page.tsx` | Page détail sujet + corrections |
| Modify | `app/examens/page.tsx` | Ajout widget "Annales récentes" |
| Modify | `components/Navbar.tsx` | Ajout lien "Annales" |

---

## Task 1 — Données annales (`lib/annales-data.ts`)

**Files:**
- Create: `lib/annales-data.ts`

- [ ] **Créer `lib/annales-data.ts` avec le contenu complet suivant :**

```typescript
export interface AnnaleQuestion {
  id: string;
  numero: string;
  intitule: string;
  bareme: number;
  correction: string;
}

export interface Annale {
  id: string;
  year: number;
  epreuve: "E4" | "E5" | "E6" | "TC";
  option: "SISR" | "SLAM" | "Commun";
  title: string;
  themes: string[];
  pdfUrl: string;
  questions: AnnaleQuestion[];
}

export const annales: Annale[] = [

  // ─── E4 — TRONC COMMUN ────────────────────────────────────────────────────

  {
    id: "e4-2024",
    year: 2024,
    epreuve: "E4",
    option: "Commun",
    title: "E4 2024 — Support et mise à disposition de services informatiques",
    themes: ["Gestion des incidents ITIL", "Déploiement de postes", "GLPI", "Documentation"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      {
        id: "e4-2024-q1",
        numero: "Question 1",
        intitule: "À partir du schéma réseau fourni, identifiez les équipements présents et précisez le rôle de chacun dans l'infrastructure de l'entreprise SOFTECH. Indiquez comment est assurée la redondance.",
        bareme: 5,
        correction: `**Éléments attendus :**\n\n- **Identification des équipements :** pare-feu (sécurité périmétrique), switch cœur (interconnexion), serveurs (AD, DNS, DHCP, fichiers), postes clients, routeur FAI\n- **Rôles :** le pare-feu filtre les flux entrants/sortants ; le switch cœur assure l'interconnexion des VLAN ; le serveur AD centralise l'authentification\n- **Redondance :** liaison agrégée (LACP) entre switchs, serveur secondaire en failover, alimentation redondante\n\n> **Conseil jury :** soigner la terminologie (ne pas confondre switch/routeur) et justifier chaque rôle avec une fonction précise.`,
      },
      {
        id: "e4-2024-q2",
        numero: "Question 2",
        intitule: "Un utilisateur signale que son poste ne démarre plus après une mise à jour Windows. Décrivez la procédure de traitement de cet incident en appliquant la démarche ITIL, depuis l'enregistrement jusqu'à la clôture.",
        bareme: 6,
        correction: `**Procédure ITIL :**\n\n1. **Enregistrement** : création du ticket dans GLPI (utilisateur, date, description, priorité)\n2. **Catégorisation** : incident matériel/logiciel – poste de travail – mise à jour\n3. **Priorisation** : impact (1 utilisateur) × urgence = priorité moyenne\n4. **Diagnostic** : démarrage en mode sans échec, journal d'événements Windows, identification KB problématique\n5. **Résolution** : désinstallation de la mise à jour via WinRE ou restauration système\n6. **Clôture** : mise à jour du ticket, documentation de la solution, information de l'utilisateur\n\n> **Point clé :** mentionner explicitement la base de connaissances GLPI pour capitaliser la solution.`,
      },
      {
        id: "e4-2024-q3",
        numero: "Question 3",
        intitule: "L'entreprise souhaite déployer 50 nouveaux postes Windows 11. Proposez une solution de déploiement automatisé en justifiant votre choix et en décrivant les étapes principales.",
        bareme: 5,
        correction: `**Solution recommandée : MDT (Microsoft Deployment Toolkit) ou WDS + réponse automatique**\n\n**Étapes :**\n1. Création d'une image de référence (master) avec sysprep\n2. Configuration du serveur MDT/WDS\n3. Création du fichier de réponses (unattend.xml)\n4. Démarrage PXE des postes cibles\n5. Déploiement automatisé + jonction au domaine AD\n6. Installation des logiciels via GPO ou SCCM\n\n**Justification :** gain de temps (déploiement simultané), cohérence des configurations, réduction des erreurs humaines.\n\n> **Alternatives acceptées :** Clonezilla, Ghost, ou solution cloud (Intune/Autopilot) avec justification.`,
      },
      {
        id: "e4-2024-q4",
        numero: "Question 4",
        intitule: "Rédigez une procédure technique destinée aux techniciens pour la création d'un compte utilisateur dans Active Directory, incluant les paramètres obligatoires et les bonnes pratiques de sécurité.",
        bareme: 4,
        correction: `**Procédure :**\n\n1. Ouvrir "Utilisateurs et ordinateurs Active Directory"\n2. Naviguer dans l'OU appropriée (ex: OU=Utilisateurs,DC=softech,DC=local)\n3. Clic droit > Nouvel utilisateur\n4. Renseigner : Prénom, Nom, **Nom de connexion** (prenom.nom@domaine)\n5. Définir mot de passe selon politique : 12 car. min., complexité activée\n6. Cocher "L'utilisateur doit changer le MDP à la prochaine ouverture"\n7. Ajouter aux groupes AD appropriés selon le profil\n8. Affecter un profil itinérant si nécessaire\n\n**Bonnes pratiques :** principe du moindre privilège, désactiver le compte en cas d'absence prolongée, audit des créations de comptes activé.`,
      },
    ],
  },

  {
    id: "e4-2023",
    year: 2023,
    epreuve: "E4",
    option: "Commun",
    title: "E4 2023 — Support et mise à disposition de services informatiques",
    themes: ["Virtualisation", "Sauvegarde", "Assistance utilisateurs", "Parc informatique"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      {
        id: "e4-2023-q1",
        numero: "Question 1",
        intitule: "L'entreprise MEDIAPRO dispose d'une infrastructure virtualisée sous VMware ESXi. Expliquez les avantages de la virtualisation pour cette entreprise et décrivez les ressources allouées à une VM de serveur de fichiers (CPU, RAM, stockage).",
        bareme: 5,
        correction: `**Avantages de la virtualisation :**\n- Réduction des coûts matériels (consolidation des serveurs)\n- Facilité de sauvegarde (snapshots)\n- Haute disponibilité (vMotion, HA)\n- Isolation des environnements\n- Rapidité de déploiement de nouveaux services\n\n**Configuration typique serveur de fichiers :**\n- CPU : 2 vCPU (suffisant pour NFS/SMB)\n- RAM : 4–8 Go\n- Stockage : disque système 60 Go + datastore dédié selon volume (ex: 500 Go)\n- Réseau : 1 vNIC en VMXNET3\n\n> **Attention :** justifier chaque choix en fonction de la charge prévisible.`,
      },
      {
        id: "e4-2023-q2",
        numero: "Question 2",
        intitule: "Proposez et justifiez une politique de sauvegarde pour les données critiques de MEDIAPRO (60 To de données), en appliquant la règle 3-2-1 et en précisant les outils utilisés.",
        bareme: 5,
        correction: `**Règle 3-2-1 :**\n- **3** copies des données\n- sur **2** supports différents (ex: disque local NAS + bande LTO)\n- dont **1** copie hors site (cloud S3, bande externalisée, datacenter distant)\n\n**Plan de sauvegarde :**\n| Type | Fréquence | Durée de rétention |\n|------|-----------|--------------------|\n| Incrémentale | Quotidienne | 7 jours |\n| Différentielle | Hebdomadaire | 4 semaines |\n| Complète | Mensuelle | 12 mois |\n\n**Outils :** Veeam Backup, Bacula, ou Amanda. Tests de restauration trimestriels obligatoires.\n\n> **Point clé :** mentionner le RTO et RPO souhaités pour l'entreprise.`,
      },
      {
        id: "e4-2023-q3",
        numero: "Question 3",
        intitule: "Un utilisateur distant se plaint de lenteurs lors de l'accès aux applications hébergées au siège. Citez les outils de diagnostic réseau que vous utiliseriez et décrivez la démarche méthodique d'investigation.",
        bareme: 4,
        correction: `**Outils de diagnostic :**\n- \`ping\` : test de connectivité et latence\n- \`tracert\` / \`traceroute\` : identification du saut problématique\n- \`pathping\` : combinaison ping + traceroute avec statistiques\n- Wireshark : analyse de trames réseau\n- Test de débit (Speedtest, iperf)\n\n**Démarche :**\n1. Vérifier la connectivité locale (ping passerelle)\n2. Tester la résolution DNS\n3. Tracert vers le serveur distant → identifier le goulot d'étranglement\n4. Mesurer le débit réel vs contractuel\n5. Vérifier la charge côté serveur (CPU, RAM, I/O)\n6. Analyser les logs du VPN si connexion via VPN`,
      },
      {
        id: "e4-2023-q4",
        numero: "Question 4",
        intitule: "Décrivez comment vous géreriez l'inventaire du parc informatique de 200 postes avec GLPI, en précisant les informations collectées et comment automatiser cette collecte.",
        bareme: 6,
        correction: `**Gestion du parc avec GLPI :**\n\n**Informations collectées :**\n- Matériel : marque, modèle, N° série, date d'achat, garantie\n- Logiciels installés, licences\n- Localisation, utilisateur affecté\n- Historique des interventions\n\n**Automatisation via plugin Fusion Inventory / OCS Inventory NG :**\n1. Déploiement de l'agent sur chaque poste (via GPO)\n2. L'agent remonte automatiquement l'inventaire à GLPI\n3. Fréquence configurable (quotidienne recommandée)\n4. Alertes automatiques : fin de garantie, logiciel non autorisé\n\n**Avantages :** gain de temps, inventaire toujours à jour, traçabilité des modifications.`,
      },
    ],
  },

  {
    id: "e4-2022",
    year: 2022,
    epreuve: "E4",
    option: "Commun",
    title: "E4 2022 — Support et mise à disposition de services informatiques",
    themes: ["Active Directory", "DHCP/DNS", "Helpdesk", "Sécurité des postes"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e4-2022-q1", numero: "Question 1", intitule: "L'entreprise LOGITRANS compte 150 utilisateurs répartis sur 3 sites. Proposez un plan d'adressage IP avec des sous-réseaux distincts par site et par usage (serveurs, postes, imprimantes, VoIP), en utilisant le bloc 192.168.0.0/16.", bareme: 6,
        correction: `**Plan d'adressage proposé :**\n\n| Sous-réseau | Plage | Usage | Nb hôtes |\n|-------------|-------|-------|----------|\n| 192.168.1.0/24 | .1–.254 | Serveurs Site A | 254 |\n| 192.168.10.0/24 | .1–.254 | Postes Site A | 254 |\n| 192.168.20.0/24 | .1–.254 | Postes Site B | 254 |\n| 192.168.30.0/24 | .1–.254 | Postes Site C | 254 |\n| 192.168.100.0/26 | .1–.62 | Imprimantes | 62 |\n| 192.168.200.0/25 | .1–.126 | VoIP | 126 |\n\n**Justification :** segmentation par usage = meilleure sécurité + QoS possible sur VLAN VoIP.` },
      { id: "e4-2022-q2", numero: "Question 2", intitule: "Expliquez le rôle des serveurs DHCP et DNS dans l'infrastructure de LOGITRANS. Que se passe-t-il si le serveur DHCP tombe en panne ? Proposez une solution de haute disponibilité.", bareme: 5,
        correction: `**DHCP :** attribution automatique des configurations IP (adresse, masque, passerelle, DNS)\n**DNS :** résolution des noms de domaine en adresses IP (et inversement)\n\n**En cas de panne DHCP :**\n- Les baux existants restent valides jusqu'à expiration\n- Nouveaux postes ne peuvent pas obtenir de configuration\n- Possible utilisation de l'adresse APIPA (169.254.x.x) — pas de connectivité réseau\n\n**Solution haute disponibilité :**\n- **DHCP Failover** (Windows Server 2012+) : serveur principal + secondaire en mode Hot Standby ou Load Balance\n- Partage du pool d'adresses (ex: 80/20)\n- Synchronisation automatique des baux\n- Bascule transparente en cas de panne` },
      { id: "e4-2022-q3", numero: "Question 3", intitule: "Rédigez le contenu d'une GPO de sécurité à déployer sur tous les postes clients de LOGITRANS pour renforcer leur sécurité, en couvrant au minimum 5 paramètres distincts.", bareme: 4,
        correction: `**GPO de sécurité — paramètres recommandés :**\n\n1. **Politique de mots de passe :** longueur min. 12 car., complexité activée, expiration 90 jours\n2. **Verrouillage de compte :** 5 tentatives, durée 30 min\n3. **Écran de veille avec MDP :** activation après 10 min d'inactivité\n4. **Windows Update :** installation automatique, redémarrage programmé\n5. **Pare-feu Windows :** activé, profils Domain/Private/Public configurés\n6. **BitLocker :** chiffrement du disque système activé\n7. **Restriction d'installation logiciels :** utilisateurs standards ne peuvent pas installer de programmes\n8. **Audit :** journalisation des connexions/déconnexions et des accès aux fichiers sensibles` },
    ],
  },

  {
    id: "e4-2021",
    year: 2021,
    epreuve: "E4",
    option: "Commun",
    title: "E4 2021 — Support et mise à disposition de services informatiques",
    themes: ["Télétravail", "VPN", "MDM", "Gestion des incidents"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e4-2021-q1", numero: "Question 1", intitule: "Suite à la crise sanitaire, l'entreprise INNOV doit déployer une solution de télétravail sécurisé pour 80 collaborateurs. Proposez une architecture technique complète en justifiant chaque composant.", bareme: 7,
        correction: `**Architecture de télétravail sécurisé :**\n\n1. **VPN SSL/TLS** (OpenVPN ou Cisco AnyConnect) : tunnel chiffré entre poste distant et SI\n2. **Authentification MFA** : login + mot de passe + OTP (Google Authenticator)\n3. **MDM** (Mobile Device Management) : contrôle des postes distants, chiffrement obligatoire, effacement à distance\n4. **Bureau virtuel (VDI)** optionnel : Citrix/RDS pour les applications sensibles\n5. **CASB / Proxy inversé** : contrôle des accès aux applications cloud\n6. **Surveillance** : SIEM pour détecter les comportements anormaux\n\n**Politique d'utilisation :** charte de télétravail, restriction des périphériques USB, interdiction des réseaux WiFi publics sans VPN.` },
      { id: "e4-2021-q2", numero: "Question 2", intitule: "Un collaborateur signale avoir reçu un email suspect lui demandant ses identifiants. Décrivez la procédure de réponse à cet incident de phishing, les mesures immédiates et préventives.", bareme: 5,
        correction: `**Mesures immédiates :**\n1. Isoler le poste du réseau si l'utilisateur a cliqué\n2. Changer immédiatement les mots de passe compromis\n3. Révoquer les sessions actives\n4. Analyser le poste (antivirus, recherche de malware)\n5. Notifier le RSSI et enregistrer l'incident\n\n**Investigation :**\n- Analyser les en-têtes de l'email (SPF, DKIM, DMARC)\n- Vérifier les logs d'authentification AD\n- Identifier si d'autres utilisateurs ont reçu le même email\n\n**Mesures préventives :**\n- Sensibilisation des utilisateurs (formation phishing)\n- Filtrage anti-spam renforcé\n- Activation de l'authentification multi-facteurs\n- Simulation de phishing périodique` },
    ],
  },

  {
    id: "e4-2020",
    year: 2020,
    epreuve: "E4",
    option: "Commun",
    title: "E4 2020 — Support et mise à disposition de services informatiques",
    themes: ["Câblage réseau", "VLAN", "Serveur de fichiers", "Sauvegarde"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e4-2020-q1", numero: "Question 1", intitule: "L'entreprise BUILDCO installe ses nouveaux locaux. Proposez un plan de câblage réseau structuré pour 3 étages (20 postes par étage), en respectant la norme TIA-568 et en précisant le matériel nécessaire.", bareme: 6,
        correction: `**Plan de câblage structuré :**\n\n**Norme TIA-568 :** câble Cat6A minimum, longueur max 90m horizontal\n\n**Architecture :**\n- **Salle serveurs (RJ45)** : baie principale, patch panels, switches cœur\n- **Armoires de brassage par étage** : switch de distribution, patch panel 24 ports\n- **Prises RJ45** : minimum 2 prises par poste\n\n**Matériel :**\n- 3 switches 48 ports PoE (1 par étage)\n- 1 switch cœur 24 ports\n- Câble Cat6A blindé (STP) : ~2000m total\n- 3 × patch panel 24 ports\n- Baie 42U pour salle serveurs\n- Goulottes et chemins de câbles` },
    ],
  },

  {
    id: "e4-2019",
    year: 2019,
    epreuve: "E4",
    option: "Commun",
    title: "E4 2019 — Support et mise à disposition de services informatiques",
    themes: ["Architecture réseau", "Serveurs Windows", "Licences", "SLA"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e4-2019-q1", numero: "Question 1", intitule: "L'entreprise COMPTA+ migre vers Windows Server 2019. Listez les rôles serveurs à installer pour une infrastructure standard PME (authentification, résolution de noms, partage de fichiers, impression) et justifiez chaque choix.", bareme: 5,
        correction: `**Rôles Windows Server essentiels :**\n\n| Rôle | Justification |\n|------|---------------|\n| **AD DS** (Active Directory) | Centralise authentification et gestion des objets (utilisateurs, GPO) |\n| **DNS** | Résolution des noms internes et externes, requis par AD |\n| **DHCP** | Attribution automatique des configurations IP |\n| **DFS / File Server** | Partage de fichiers avec espaces de noms distribués |\n| **Print Server** | Centralise la gestion des imprimantes réseau |\n| **WSUS** | Gestion centralisée des mises à jour Windows |\n\n**Recommandation :** séparer les rôles sur des VMs distinctes pour la sécurité et la performance.` },
    ],
  },

  // ─── E5 SISR ──────────────────────────────────────────────────────────────

  {
    id: "e5-sisr-2024",
    year: 2024,
    epreuve: "E5",
    option: "SISR",
    title: "E5 SISR 2024 — Administration des systèmes et des réseaux",
    themes: ["Routage OSPF", "VLAN inter-sites", "Haute disponibilité", "Supervision Nagios"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-sisr-2024-q1", numero: "Question 1", intitule: "L'entreprise NETPRO interconnecte 3 sites via MPLS. Configurez OSPF sur les routeurs Cisco pour assurer le routage dynamique inter-sites. Montrez la configuration du routeur du site principal (R1) avec 3 réseaux à annoncer.", bareme: 7,
        correction: `\`\`\`cisco\nR1# configure terminal\nR1(config)# router ospf 1\nR1(config-router)# router-id 1.1.1.1\nR1(config-router)# network 192.168.10.0 0.0.0.255 area 0\nR1(config-router)# network 192.168.20.0 0.0.0.255 area 0\nR1(config-router)# network 10.0.0.0 0.0.0.3 area 0\nR1(config-router)# passive-interface GigabitEthernet0/1\nR1(config-router)# auto-cost reference-bandwidth 1000\nR1(config-router)# end\nR1# show ip ospf neighbor\nR1# show ip route ospf\n\`\`\`\n\n**Points clés :**\n- \`router-id\` unique par routeur\n- Wildcard mask = inverse du masque de sous-réseau\n- \`passive-interface\` sur les interfaces sans voisin OSPF (LAN)\n- \`auto-cost\` à ajuster si liens >100 Mbps\n- Vérifier l'adjacency avec \`show ip ospf neighbor\`` },
      { id: "e5-sisr-2024-q2", numero: "Question 2", intitule: "Configurez des VLANs sur un switch Cisco Catalyst pour séparer le trafic : VLAN 10 (Direction), VLAN 20 (Commercial), VLAN 30 (IT), avec un trunk vers le routeur pour le routage inter-VLAN (Router-on-a-Stick).", bareme: 6,
        correction: `**Switch :**\n\`\`\`cisco\nSW1# configure terminal\nSW1(config)# vlan 10\nSW1(config-vlan)# name Direction\nSW1(config-vlan)# vlan 20\nSW1(config-vlan)# name Commercial\nSW1(config-vlan)# vlan 30\nSW1(config-vlan)# name IT\nSW1(config-vlan)# exit\nSW1(config)# interface range fa0/1-8\nSW1(config-if-range)# switchport mode access\nSW1(config-if-range)# switchport access vlan 10\nSW1(config)# interface GigabitEthernet0/1\nSW1(config-if)# switchport mode trunk\nSW1(config-if)# switchport trunk allowed vlan 10,20,30\n\`\`\`\n\n**Routeur (sous-interfaces) :**\n\`\`\`cisco\nR1(config)# interface GigabitEthernet0/0.10\nR1(config-subif)# encapsulation dot1Q 10\nR1(config-subif)# ip address 192.168.10.1 255.255.255.0\nR1(config)# interface GigabitEthernet0/0.20\nR1(config-subif)# encapsulation dot1Q 20\nR1(config-subif)# ip address 192.168.20.1 255.255.255.0\n\`\`\`` },
      { id: "e5-sisr-2024-q3", numero: "Question 3", intitule: "Mettez en place une supervision réseau avec Nagios/Centreon. Quels hôtes et services superviseriez-vous en priorité ? Décrivez la configuration d'une alerte email pour un service critique.", bareme: 5,
        correction: `**Hôtes prioritaires :** routeurs, switchs cœur, serveurs (AD, DNS, DHCP, fichiers), pare-feu, accès internet\n\n**Services prioritaires :**\n- \`check_ping\` : disponibilité des hôtes\n- \`check_http\` : serveurs web\n- \`check_disk\` : espace disque (seuil 80%/95%)\n- \`check_load\` : charge CPU\n- \`check_smtp\`, \`check_dns\` : services réseau\n\n**Configuration alerte email (Nagios) :**\n\`\`\`cfg\ndefine contact {\n  contact_name            admin\n  email                   admin@entreprise.fr\n  service_notification_commands  notify-service-by-email\n  host_notification_commands     notify-host-by-email\n  service_notification_options   w,u,c,r\n  host_notification_options      d,u,r\n}\n\`\`\`` },
    ],
  },

  {
    id: "e5-sisr-2023",
    year: 2023,
    epreuve: "E5",
    option: "SISR",
    title: "E5 SISR 2023 — Administration des systèmes et des réseaux",
    themes: ["Linux Debian", "Services réseau", "Scripting Bash", "Active Directory Federation"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-sisr-2023-q1", numero: "Question 1", intitule: "Sur un serveur Debian 12, configurez un serveur DHCP avec isc-dhcp-server pour le réseau 192.168.50.0/24. Réservez l'adresse 192.168.50.10 pour un serveur d'impression (MAC : AA:BB:CC:DD:EE:FF).", bareme: 6,
        correction: `\`\`\`bash\n# Installation\napt install isc-dhcp-server -y\n\n# /etc/dhcp/dhcpd.conf\nsubnet 192.168.50.0 netmask 255.255.255.0 {\n  range 192.168.50.100 192.168.50.200;\n  option domain-name-servers 192.168.50.1;\n  option domain-name "lan.local";\n  option routers 192.168.50.254;\n  default-lease-time 86400;\n  max-lease-time 604800;\n}\n\n# Réservation imprimante\nhost imprimante {\n  hardware ethernet AA:BB:CC:DD:EE:FF;\n  fixed-address 192.168.50.10;\n}\n\n# Interface d'écoute : /etc/default/isc-dhcp-server\nINTERFACESv4="eth0"\n\nsystemctl restart isc-dhcp-server\nsystemctl enable isc-dhcp-server\n\`\`\`` },
      { id: "e5-sisr-2023-q2", numero: "Question 2", intitule: "Écrivez un script Bash qui surveille l'espace disque de toutes les partitions et envoie un email d'alerte si l'utilisation dépasse 80%. Le script doit être exécuté quotidiennement via cron.", bareme: 5,
        correction: `\`\`\`bash\n#!/bin/bash\n# /usr/local/bin/check_disk.sh\n\nSEUIL=80\nDESTINATAIRE="admin@entreprise.fr"\nSUJET="ALERTE - Espace disque critique"\n\nALERTE=""\n\nwhile IFS= read -r ligne; do\n  USAGE=$(echo "$ligne" | awk '{print $5}' | tr -d '%')\n  PARTITION=$(echo "$ligne" | awk '{print $6}')\n  \n  if [ "$USAGE" -gt "$SEUIL" ]; then\n    ALERTE+="Partition $PARTITION : ${USAGE}% utilisé\\n"\n  fi\ndone < <(df -h | tail -n +2)\n\nif [ -n "$ALERTE" ]; then\n  echo -e "$ALERTE" | mail -s "$SUJET" "$DESTINATAIRE"\nfi\n\`\`\`\n\n**Cron (quotidien 8h) :**\n\`\`\`bash\n0 8 * * * /usr/local/bin/check_disk.sh\n\`\`\`` },
    ],
  },

  {
    id: "e5-sisr-2022",
    year: 2022,
    epreuve: "E5",
    option: "SISR",
    title: "E5 SISR 2022 — Administration des systèmes et des réseaux",
    themes: ["Windows Server 2019", "AD DS", "GPO avancées", "DFS"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-sisr-2022-q1", numero: "Question 1", intitule: "Configurez un DFS (Distributed File System) Namespace sur Windows Server 2019 pour centraliser l'accès aux partages de fichiers répartis sur 3 serveurs. Décrivez les étapes et la structure proposée.", bareme: 6,
        correction: `**Étapes de configuration DFS :**\n\n1. Installer le rôle DFS (Namespaces + Réplication) via Server Manager\n2. Créer un namespace de domaine : \`\\\\domaine.local\\Partages\`\n3. Ajouter des dossiers DFS pointant vers les partages existants :\n   - \`\\\\domaine.local\\Partages\\Direction\` → \`\\\\SRV01\\Direction$\`\n   - \`\\\\domaine.local\\Partages\\Commercial\` → \`\\\\SRV02\\Commercial$\`\n   - \`\\\\domaine.local\\Partages\\Commun\` → \`\\\\SRV01\\Commun$\`, \`\\\\SRV02\\Commun$\` (réplication)\n4. Configurer la réplication DFS-R pour les dossiers redondants\n5. Définir la topologie de réplication (Full Mesh ou Hub-and-Spoke)\n\n**Avantages :** transparence pour les utilisateurs, haute disponibilité, load balancing.` },
    ],
  },

  {
    id: "e5-sisr-2021",
    year: 2021,
    epreuve: "E5",
    option: "SISR",
    title: "E5 SISR 2021 — Administration des systèmes et des réseaux",
    themes: ["Cluster haute disponibilité", "Load Balancing", "Monitoring", "IPv6"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-sisr-2021-q1", numero: "Question 1", intitule: "Expliquez le fonctionnement du protocole HSRP (Hot Standby Router Protocol) et configurez-le sur deux routeurs Cisco pour assurer la redondance de la passerelle par défaut du réseau 192.168.1.0/24.", bareme: 6,
        correction: `**Fonctionnement HSRP :**\nHSRP crée une passerelle virtuelle partagée entre 2 routeurs. Un routeur est Actif, l'autre Standby. Les clients utilisent l'IP virtuelle. En cas de panne du routeur actif, le Standby prend le relais automatiquement.\n\n**Configuration :**\n\`\`\`cisco\n! Routeur principal (priorité haute)\nR1(config)# interface GigabitEthernet0/0\nR1(config-if)# ip address 192.168.1.1 255.255.255.0\nR1(config-if)# standby 1 ip 192.168.1.254\nR1(config-if)# standby 1 priority 110\nR1(config-if)# standby 1 preempt\nR1(config-if)# standby 1 authentication md5 key-string SECRET\n\n! Routeur secondaire\nR2(config)# interface GigabitEthernet0/0\nR2(config-if)# ip address 192.168.1.2 255.255.255.0\nR2(config-if)# standby 1 ip 192.168.1.254\nR2(config-if)# standby 1 priority 90\n\`\`\`\n\n**IP virtuelle 192.168.1.254** = passerelle des clients` },
    ],
  },

  {
    id: "e5-sisr-2020",
    year: 2020,
    epreuve: "E5",
    option: "SISR",
    title: "E5 SISR 2020 — Administration des systèmes et des réseaux",
    themes: ["Calcul de sous-réseaux", "NAT/PAT", "Services Linux", "Proxy Squid"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-sisr-2020-q1", numero: "Question 1", intitule: "Découpez le réseau 172.16.0.0/20 en 8 sous-réseaux de taille égale. Pour chaque sous-réseau, indiquez l'adresse réseau, le masque, la première et dernière adresse utilisable, et l'adresse de diffusion.", bareme: 6,
        correction: `**Calcul :** /20 = 4096 adresses. 8 sous-réseaux → /23 (512 adresses, 510 hôtes)\n\n| Sous-réseau | Réseau | Première IP | Dernière IP | Broadcast |\n|-------------|--------|-------------|-------------|-----------|\n| 1 | 172.16.0.0/23 | 172.16.0.1 | 172.16.1.254 | 172.16.1.255 |\n| 2 | 172.16.2.0/23 | 172.16.2.1 | 172.16.3.254 | 172.16.3.255 |\n| 3 | 172.16.4.0/23 | 172.16.4.1 | 172.16.5.254 | 172.16.5.255 |\n| 4 | 172.16.6.0/23 | 172.16.6.1 | 172.16.7.254 | 172.16.7.255 |\n| 5 | 172.16.8.0/23 | 172.16.8.1 | 172.16.9.254 | 172.16.9.255 |\n| 6 | 172.16.10.0/23 | 172.16.10.1 | 172.16.11.254 | 172.16.11.255 |\n| 7 | 172.16.12.0/23 | 172.16.12.1 | 172.16.13.254 | 172.16.13.255 |\n| 8 | 172.16.14.0/23 | 172.16.14.1 | 172.16.15.254 | 172.16.15.255 |` },
    ],
  },

  {
    id: "e5-sisr-2019",
    year: 2019,
    epreuve: "E5",
    option: "SISR",
    title: "E5 SISR 2019 — Administration des systèmes et des réseaux",
    themes: ["PKI / Certificats SSL", "IIS", "Active Directory", "Scripting PowerShell"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-sisr-2019-q1", numero: "Question 1", intitule: "Expliquez le principe des certificats SSL/TLS et d'une PKI (Public Key Infrastructure). Décrivez les étapes pour créer une autorité de certification interne sous Windows Server et émettre un certificat pour un serveur web IIS.", bareme: 7,
        correction: `**PKI - Principes :**\n- Repose sur la cryptographie asymétrique (clé publique/privée)\n- L'AC (Autorité de Certification) signe les certificats et garantit l'identité\n- Chaîne de confiance : AC Racine → AC Intermédiaire → Certificat final\n\n**Étapes Windows Server :**\n1. Installer le rôle "Services de certificats Active Directory"\n2. Configurer l'AC Racine d'entreprise (CA d'entreprise intégrée à AD)\n3. Sur le serveur IIS : créer une demande de certificat (CSR)\n4. Soumettre le CSR à l'AC via le gestionnaire web certsrv\n5. Télécharger et installer le certificat sur IIS\n6. Lier le certificat au site HTTPS (port 443)\n7. Déployer le certificat racine via GPO sur tous les postes (Trusted Root CA)\n\n\`\`\`powershell\n# Vérifier le certificat installé\nGet-ChildItem Cert:\\LocalMachine\\My | Select-Object Subject, Thumbprint, NotAfter\n\`\`\`` },
    ],
  },

  // ─── E5 SLAM ──────────────────────────────────────────────────────────────

  {
    id: "e5-slam-2024",
    year: 2024,
    epreuve: "E5",
    option: "SLAM",
    title: "E5 SLAM 2024 — Conception et développement d'applications",
    themes: ["API REST sécurisée", "JWT", "Base de données relationnelle", "Tests unitaires"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-slam-2024-q1", numero: "Question 1", intitule: "Concevez le MCD (Modèle Conceptuel de Données) d'une application de gestion de bibliothèque. Le système doit gérer : les livres (ISBN, titre, auteurs), les adhérents, les emprunts (date début/fin, prolongation possible), et les réservations.", bareme: 7,
        correction: `**Entités et attributs :**\n\n- **LIVRE** : ISBN (PK), titre, date_publication, genre, stock_total\n- **AUTEUR** : id_auteur (PK), nom, prenom, nationalite\n- **ADHÉRENT** : id_adherent (PK), nom, prenom, email, date_inscription, statut\n- **EMPRUNT** : id_emprunt (PK), date_debut, date_retour_prevue, date_retour_reelle, prolonge\n- **RÉSERVATION** : id_reservation (PK), date_reservation, statut\n\n**Associations :**\n- LIVRE ←(écrire)→ AUTEUR : N,N (un livre peut avoir plusieurs auteurs)\n- ADHÉRENT ←(emprunter)→ LIVRE : 1,N (un emprunt concerne 1 livre)\n- ADHÉRENT ←(réserver)→ LIVRE : 1,N\n\n**Règles de gestion :**\n- Un adhérent ne peut pas emprunter si retard non soldé\n- Maximum 3 emprunts simultanés par adhérent\n- Prolongation possible 1 fois si pas de réservation en attente` },
      { id: "e5-slam-2024-q2", numero: "Question 2", intitule: "Écrivez les requêtes SQL permettant : (a) d'afficher les 10 livres les plus empruntés sur les 12 derniers mois avec leur nombre d'emprunts, (b) les adhérents ayant des emprunts en retard de plus de 30 jours.", bareme: 6,
        correction: `\`\`\`sql\n-- (a) 10 livres les plus empruntés (12 derniers mois)\nSELECT l.ISBN, l.titre, COUNT(e.id_emprunt) AS nb_emprunts\nFROM LIVRE l\nINNER JOIN EMPRUNT e ON l.ISBN = e.ISBN\nWHERE e.date_debut >= DATE_SUB(CURRENT_DATE, INTERVAL 12 MONTH)\nGROUP BY l.ISBN, l.titre\nORDER BY nb_emprunts DESC\nLIMIT 10;\n\n-- (b) Adhérents avec emprunts en retard > 30 jours\nSELECT a.nom, a.prenom, a.email,\n       e.date_retour_prevue,\n       DATEDIFF(CURRENT_DATE, e.date_retour_prevue) AS jours_retard\nFROM ADHERENT a\nINNER JOIN EMPRUNT e ON a.id_adherent = e.id_adherent\nWHERE e.date_retour_reelle IS NULL\n  AND DATEDIFF(CURRENT_DATE, e.date_retour_prevue) > 30\nORDER BY jours_retard DESC;\n\`\`\`` },
      { id: "e5-slam-2024-q3", numero: "Question 3", intitule: "Développez en PHP une API REST pour l'endpoint GET /api/livres avec pagination (paramètres page et limit), filtrage par genre, et authentification par token JWT. Retournez le résultat en JSON.", bareme: 7,
        correction: `\`\`\`php\n<?php\n// api/livres.php\nheader('Content-Type: application/json');\n\n// Vérification JWT\nfunction verifyJWT(string $token): bool {\n    [$header, $payload, $sig] = explode('.', $token);\n    $expected = base64_encode(hash_hmac('sha256', "$header.$payload", JWT_SECRET, true));\n    return hash_equals($expected, $sig);\n}\n\n$authHeader = $_SERVER['HTTP_AUTHORIZATION'] ?? '';\nif (!preg_match('/Bearer (.+)/', $authHeader, $m) || !verifyJWT($m[1])) {\n    http_response_code(401);\n    echo json_encode(['error' => 'Non autorisé']);\n    exit;\n}\n\n$page  = max(1, (int)($_GET['page'] ?? 1));\n$limit = min(50, max(1, (int)($_GET['limit'] ?? 10)));\n$genre = $_GET['genre'] ?? null;\n$offset = ($page - 1) * $limit;\n\n$pdo = new PDO(DSN, DB_USER, DB_PASS, [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]);\n\n$sql = 'SELECT ISBN, titre, genre FROM LIVRE WHERE 1=1';\n$params = [];\nif ($genre) { $sql .= ' AND genre = :genre'; $params[':genre'] = $genre; }\n$sql .= ' LIMIT :limit OFFSET :offset';\n\n$stmt = $pdo->prepare($sql);\nforeach ($params as $k => $v) $stmt->bindValue($k, $v);\n$stmt->bindValue(':limit', $limit, PDO::PARAM_INT);\n$stmt->bindValue(':offset', $offset, PDO::PARAM_INT);\n$stmt->execute();\n\necho json_encode(['data' => $stmt->fetchAll(PDO::FETCH_ASSOC), 'page' => $page, 'limit' => $limit]);\n\`\`\`` },
    ],
  },

  {
    id: "e5-slam-2023",
    year: 2023,
    epreuve: "E5",
    option: "SLAM",
    title: "E5 SLAM 2023 — Conception et développement d'applications",
    themes: ["POO PHP", "MVC", "Formulaires sécurisés", "SQL injection"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-slam-2023-q1", numero: "Question 1", intitule: "Créez une classe PHP Produit avec les attributs id, nom, prix, stock. Ajoutez une classe ProduitRepository avec les méthodes findAll(), findById(int $id) et save(Produit $p) utilisant PDO. Respectez les principes POO (encapsulation, constructeur).", bareme: 7,
        correction: `\`\`\`php\n<?php\nclass Produit {\n    private int $id;\n    private string $nom;\n    private float $prix;\n    private int $stock;\n    \n    public function __construct(string $nom, float $prix, int $stock, int $id = 0) {\n        $this->nom = $nom;\n        $this->prix = $prix;\n        $this->stock = $stock;\n        $this->id = $id;\n    }\n    \n    public function getId(): int { return $this->id; }\n    public function getNom(): string { return $this->nom; }\n    public function getPrix(): float { return $this->prix; }\n    public function getStock(): int { return $this->stock; }\n    public function setStock(int $s): void { $this->stock = max(0, $s); }\n}\n\nclass ProduitRepository {\n    public function __construct(private PDO $pdo) {}\n    \n    public function findAll(): array {\n        $stmt = $this->pdo->query('SELECT * FROM produit');\n        return array_map(fn($r) => new Produit($r['nom'], $r['prix'], $r['stock'], $r['id']), $stmt->fetchAll());\n    }\n    \n    public function findById(int $id): ?Produit {\n        $stmt = $this->pdo->prepare('SELECT * FROM produit WHERE id = ?');\n        $stmt->execute([$id]);\n        $r = $stmt->fetch();\n        return $r ? new Produit($r['nom'], $r['prix'], $r['stock'], $r['id']) : null;\n    }\n    \n    public function save(Produit $p): void {\n        if ($p->getId() === 0) {\n            $stmt = $this->pdo->prepare('INSERT INTO produit (nom, prix, stock) VALUES (?, ?, ?)');\n            $stmt->execute([$p->getNom(), $p->getPrix(), $p->getStock()]);\n        } else {\n            $stmt = $this->pdo->prepare('UPDATE produit SET stock = ? WHERE id = ?');\n            $stmt->execute([$p->getStock(), $p->getId()]);\n        }\n    }\n}\n\`\`\`` },
    ],
  },

  {
    id: "e5-slam-2022",
    year: 2022,
    epreuve: "E5",
    option: "SLAM",
    title: "E5 SLAM 2022 — Conception et développement d'applications",
    themes: ["JavaScript ES6+", "Fetch API", "UML", "Tests PHPUnit"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-slam-2022-q1", numero: "Question 1", intitule: "Développez en JavaScript une fonction asynchrone qui récupère une liste de produits depuis l'API /api/produits, gère les erreurs réseau et HTTP, et affiche les résultats dans un tableau HTML dynamiquement.", bareme: 6,
        correction: `\`\`\`javascript\nasync function chargerProduits() {\n  const tbody = document.querySelector('#tableau-produits tbody');\n  tbody.innerHTML = '<tr><td colspan="3">Chargement...</td></tr>';\n  \n  try {\n    const response = await fetch('/api/produits', {\n      headers: { 'Accept': 'application/json' }\n    });\n    \n    if (!response.ok) {\n      throw new Error(\`Erreur HTTP : \${response.status}\`);\n    }\n    \n    const produits = await response.json();\n    \n    tbody.innerHTML = produits.map(p => \`\n      <tr>\n        <td>\${p.id}</td>\n        <td>\${escapeHtml(p.nom)}</td>\n        <td>\${p.prix.toFixed(2)} €</td>\n      </tr>\n    \`).join('');\n    \n  } catch (err) {\n    tbody.innerHTML = \`<tr><td colspan="3" class="erreur">Erreur : \${err.message}</td></tr>\`;\n    console.error('Erreur chargement produits:', err);\n  }\n}\n\nfunction escapeHtml(str) {\n  return str.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');\n}\n\ndocument.addEventListener('DOMContentLoaded', chargerProduits);\n\`\`\`` },
    ],
  },

  {
    id: "e5-slam-2021",
    year: 2021,
    epreuve: "E5",
    option: "SLAM",
    title: "E5 SLAM 2021 — Conception et développement d'applications",
    themes: ["Architecture MVC", "Sessions PHP", "Diagramme de séquence", "Normalisation BDD"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-slam-2021-q1", numero: "Question 1", intitule: "Décrivez l'architecture MVC (Modèle-Vue-Contrôleur) et expliquez comment s'organise le traitement d'une requête HTTP POST de connexion utilisateur dans ce pattern. Illustrez avec un exemple de code PHP.", bareme: 7,
        correction: `**Architecture MVC :**\n- **Modèle** : accès aux données (BDD), logique métier\n- **Vue** : interface utilisateur (HTML/CSS), aucune logique\n- **Contrôleur** : reçoit les requêtes, coordonne Modèle et Vue\n\n**Flux POST /login :**\n1. Router → \`AuthController::login()\`\n2. Contrôleur valide les données\n3. Contrôleur appelle \`UserModel::findByEmail()\`\n4. Modèle interroge la BDD\n5. Contrôleur vérifie le mot de passe (bcrypt)\n6. Succès : redirection Dashboard | Échec : Vue login avec erreur\n\n\`\`\`php\n// AuthController.php\npublic function login(): void {\n    if ($_SERVER['REQUEST_METHOD'] !== 'POST') { $this->render('login'); return; }\n    \n    $email = filter_input(INPUT_POST, 'email', FILTER_VALIDATE_EMAIL);\n    $password = $_POST['password'] ?? '';\n    \n    $user = $this->userModel->findByEmail($email);\n    \n    if ($user && password_verify($password, $user['password_hash'])) {\n        session_regenerate_id(true);\n        $_SESSION['user_id'] = $user['id'];\n        header('Location: /dashboard');\n    } else {\n        $this->render('login', ['error' => 'Identifiants invalides']);\n    }\n}\n\`\`\`` },
    ],
  },

  {
    id: "e5-slam-2020",
    year: 2020,
    epreuve: "E5",
    option: "SLAM",
    title: "E5 SLAM 2020 — Conception et développement d'applications",
    themes: ["Développement mobile Android", "SQLite", "RecyclerView", "Intents"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-slam-2020-q1", numero: "Question 1", intitule: "Expliquez le cycle de vie d'une Activity Android. Quelles méthodes sont appelées lors d'une rotation d'écran ? Comment préserver l'état de l'interface lors de ce changement de configuration ?", bareme: 5,
        correction: `**Cycle de vie :** onCreate → onStart → onResume → [En cours] → onPause → onStop → onDestroy\n\n**Lors d'une rotation :**\nonPause() → onStop() → **onDestroy()** → onCreate() → onStart() → onResume()\n\n*L'Activity est entièrement détruite et recrée.*\n\n**Préserver l'état :**\n\n1. **onSaveInstanceState(Bundle)** : sauvegarde avant destruction\n\`\`\`kotlin\noverride fun onSaveInstanceState(outState: Bundle) {\n    super.onSaveInstanceState(outState)\n    outState.putString("texte", editText.text.toString())\n    outState.putInt("score", score)\n}\n\`\`\`\n\n2. **Restauration dans onCreate :**\n\`\`\`kotlin\nif (savedInstanceState != null) {\n    editText.setText(savedInstanceState.getString("texte"))\n    score = savedInstanceState.getInt("score")\n}\n\`\`\`\n\n3. **Alternative moderne :** ViewModel (survit aux changements de configuration)` },
    ],
  },

  {
    id: "e5-slam-2019",
    year: 2019,
    epreuve: "E5",
    option: "SLAM",
    title: "E5 SLAM 2019 — Conception et développement d'applications",
    themes: ["UML Use Case", "Merise MCD/MLD", "SQL procédures stockées", "PHP PDO"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e5-slam-2019-q1", numero: "Question 1", intitule: "À partir d'un diagramme de cas d'utilisation UML fourni pour une application de gestion de commandes, rédigez le MCD Merise correspondant avec au minimum 4 entités et leurs associations.", bareme: 7,
        correction: `**Entités pour gestion de commandes :**\n\n**CLIENT** (id_client, nom, prenom, email, telephone, adresse)\n**COMMANDE** (id_commande, date_commande, statut, montant_total)\n**PRODUIT** (id_produit, reference, designation, prix_unitaire, stock)\n**CATEGORIE** (id_categorie, libelle)\n\n**Associations :**\n- CLIENT **passer** COMMANDE : 1,N — un client passe plusieurs commandes\n- COMMANDE **contenir** PRODUIT : N,N avec attribut quantite, prix_applique\n- PRODUIT **appartenir** CATEGORIE : N,1\n\n**MLD résultant :**\n\`\`\`sql\nCLIENT(id_client PK, nom, prenom, email)\nCOMMANDE(id_commande PK, date, statut, #id_client)\nLIGNE_COMMANDE(#id_commande, #id_produit, quantite, prix_applique) PK composite\nPRODUIT(id_produit PK, reference, designation, prix, #id_categorie)\nCATEGORIE(id_categorie PK, libelle)\n\`\`\`` },
    ],
  },

  // ─── E6 CYBERSÉCURITÉ ─────────────────────────────────────────────────────

  {
    id: "e6-2024",
    year: 2024,
    epreuve: "E6",
    option: "Commun",
    title: "E6 2024 — Cybersécurité des services informatiques",
    themes: ["OWASP Top 10", "Analyse de risques EBIOS", "Forensic", "Zero Trust"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e6-2024-q1", numero: "Question 1", intitule: "Une application web de l'entreprise CYBERCO présente une faille d'injection SQL. Démontrez l'exploitation possible d'un formulaire de connexion vulnérable, puis proposez les corrections à apporter au code PHP.", bareme: 7,
        correction: `**Exploitation (code vulnérable) :**\n\`\`\`php\n// CODE VULNÉRABLE — NE PAS UTILISER\n$query = "SELECT * FROM users WHERE login='$_POST[login]' AND pass='$_POST[pass]'";\n// Payload d'attaque : login = admin'-- \n// Requête résultante : SELECT * FROM users WHERE login='admin'--' AND pass='...'\n// Le -- commente le reste → authentification bypassée\n\`\`\`\n\n**Correction (requête préparée PDO) :**\n\`\`\`php\n// CODE SÉCURISÉ\n$stmt = $pdo->prepare('SELECT * FROM users WHERE login = :login');\n$stmt->execute([':login' => $_POST['login']]);\n$user = $stmt->fetch();\n\nif ($user && password_verify($_POST['pass'], $user['password_hash'])) {\n    // Authentification réussie\n}\n\`\`\`\n\n**Autres mesures :** validation des entrées, échappement des sorties, WAF, monitoring des requêtes SQL anormales.` },
      { id: "e6-2024-q2", numero: "Question 2", intitule: "Décrivez la méthodologie EBIOS Risk Manager pour analyser les risques SI d'une PME. Quelles sont les 5 ateliers et que produit chacun ? Illustrez avec un exemple pour CYBERCO.", bareme: 6,
        correction: `**Les 5 ateliers EBIOS RM :**\n\n1. **Atelier 1 — Cadrage et socle de sécurité**\n   → Périmètre, valeurs métiers, socle de sécurité existant\n   *Ex CYBERCO : application e-commerce, données clients, RGPD*\n\n2. **Atelier 2 — Sources de risques**\n   → Identification des attaquants potentiels et leur motivation\n   *Ex : cybercriminels (vol données CB), concurrents (espionnage), hacktivistes*\n\n3. **Atelier 3 — Scénarios stratégiques**\n   → Chemins d'attaque haut niveau\n   *Ex : compromission du CMS → accès BDD clients*\n\n4. **Atelier 4 — Scénarios opérationnels**\n   → Scénarios techniques détaillés\n   *Ex : injection SQL → dump table commandes*\n\n5. **Atelier 5 — Traitement du risque**\n   → Mesures de sécurité, risques résiduels, plan de traitement\n   *Ex : WAF + requêtes préparées + audit de code*` },
    ],
  },

  {
    id: "e6-2023",
    year: 2023,
    epreuve: "E6",
    option: "Commun",
    title: "E6 2023 — Cybersécurité des services informatiques",
    themes: ["RGPD", "Chiffrement", "Pare-feu iptables", "Audit de sécurité"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e6-2023-q1", numero: "Question 1", intitule: "L'entreprise DATASEC collecte des données personnelles de clients. Listez les obligations RGPD applicables, les droits des personnes concernées, et les mesures techniques à mettre en place.", bareme: 6,
        correction: `**Obligations RGPD :**\n- Licéité du traitement (consentement, contrat, intérêt légitime)\n- Minimisation des données collectées\n- Durée de conservation limitée\n- Registre des traitements (Art. 30)\n- DPO désigné si traitement à grande échelle\n- Notification CNIL sous 72h en cas de violation\n\n**Droits des personnes :**\n- Droit d'accès, rectification, effacement (droit à l'oubli)\n- Droit à la portabilité\n- Droit d'opposition\n- Droit à la limitation du traitement\n\n**Mesures techniques :**\n- Chiffrement des données sensibles (AES-256)\n- Pseudonymisation\n- Contrôle d'accès (RBAC)\n- Journalisation des accès\n- Tests d'intrusion réguliers\n- Politique de mots de passe forte` },
      { id: "e6-2023-q2", numero: "Question 2", intitule: "Configurez un pare-feu iptables sur un serveur Linux pour : autoriser SSH uniquement depuis le réseau interne (10.0.0.0/8), autoriser HTTP/HTTPS depuis partout, bloquer tout le reste en entrée.", bareme: 5,
        correction: `\`\`\`bash\n#!/bin/bash\n# Politique par défaut : tout bloquer\niptables -P INPUT DROP\niptables -P FORWARD DROP\niptables -P OUTPUT ACCEPT\n\n# Autoriser loopback\niptables -A INPUT -i lo -j ACCEPT\n\n# Autoriser connexions établies/liées\niptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT\n\n# SSH uniquement depuis réseau interne\niptables -A INPUT -p tcp --dport 22 -s 10.0.0.0/8 -j ACCEPT\n\n# HTTP et HTTPS depuis partout\niptables -A INPUT -p tcp --dport 80 -j ACCEPT\niptables -A INPUT -p tcp --dport 443 -j ACCEPT\n\n# Ping (optionnel)\niptables -A INPUT -p icmp --icmp-type echo-request -j ACCEPT\n\n# Sauvegarder les règles\niptables-save > /etc/iptables/rules.v4\n\`\`\`` },
    ],
  },

  {
    id: "e6-2022",
    year: 2022,
    epreuve: "E6",
    option: "Commun",
    title: "E6 2022 — Cybersécurité des services informatiques",
    themes: ["PRA/PCA", "Cryptographie asymétrique", "XSS", "Hardening Linux"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e6-2022-q1", numero: "Question 1", intitule: "Expliquez la différence entre un Plan de Reprise d'Activité (PRA) et un Plan de Continuité d'Activité (PCA). Définissez les notions de RTO et RPO. Pour un SI bancaire, proposez des valeurs typiques et les solutions techniques associées.", bareme: 6,
        correction: `**PCA vs PRA :**\n- **PCA** : maintien de l'activité pendant la crise (mode dégradé)\n- **PRA** : reprise de l'activité après interruption\n\n**RTO (Recovery Time Objective) :** durée maximale d'interruption tolérée\n**RPO (Recovery Point Objective) :** perte de données maximale tolérée (en temps)\n\n**SI Bancaire — valeurs typiques :**\n\n| Indicateur | Valeur | Solution |\n|------------|--------|----------|\n| RTO | < 1 heure | Cluster actif-actif, bascule automatique |\n| RPO | < 15 minutes | Réplication synchrone temps réel |\n\n**Solutions :**\n- Réplication synchrone BDD (Oracle DataGuard, SQL AlwaysOn)\n- Cluster haute disponibilité (Pacemaker/Corosync)\n- Site DR géographiquement distant\n- Tests de bascule semestriels obligatoires` },
    ],
  },

  {
    id: "e6-2021",
    year: 2021,
    epreuve: "E6",
    option: "Commun",
    title: "E6 2021 — Cybersécurité des services informatiques",
    themes: ["Ransomware", "SIEM", "VPN IPsec", "Sécurité WiFi WPA3"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e6-2021-q1", numero: "Question 1", intitule: "L'entreprise SECUREX a été victime d'un ransomware. Décrivez les étapes de réponse à cet incident, de la détection à la reprise d'activité, ainsi que les mesures préventives à mettre en place.", bareme: 7,
        correction: `**Réponse à l'incident :**\n\n**1. Détection et confinement**\n- Isoler immédiatement les machines infectées (déconnecter du réseau)\n- Identifier le périmètre de propagation (logs réseau, AD)\n- Préserver les preuves (snapshot mémoire, disque)\n\n**2. Éradication**\n- Identifier la souche ransomware (ID Ransomware)\n- Nettoyer les machines infectées ou les restaurer\n- Changer tous les mots de passe (AD compromis probable)\n\n**3. Reprise**\n- Restaurer depuis sauvegardes saines (vérifier date avant infection)\n- Tester l'intégrité des données restaurées\n- Reprendre progressivement les services\n\n**Mesures préventives :**\n- Sauvegardes 3-2-1 avec copie hors ligne (air-gap)\n- Segmentation réseau (limiter la propagation)\n- EDR sur tous les postes\n- Patch management régulier\n- Formation utilisateurs (phishing = vecteur principal)` },
    ],
  },

  {
    id: "e6-2020",
    year: 2020,
    epreuve: "E6",
    option: "Commun",
    title: "E6 2020 — Cybersécurité des services informatiques",
    themes: ["Tests d'intrusion", "Nmap", "Metasploit", "Rapport d'audit"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e6-2020-q1", numero: "Question 1", intitule: "Dans le cadre d'un test d'intrusion autorisé, décrivez les 5 phases méthodologiques d'un pentest selon le standard PTES (Penetration Testing Execution Standard). Pour chaque phase, donnez un exemple concret d'outil utilisé.", bareme: 6,
        correction: `**5 phases PTES :**\n\n1. **Reconnaissance (passive)** : collecte d'informations sans interaction directe\n   → *Outils : Maltego, Shodan, OSINT Framework, theHarvester*\n\n2. **Scanning / Énumération** : découverte des systèmes et services exposés\n   → *Outils : Nmap (\`nmap -sV -sC -O cible\`), Masscan, Nikto*\n\n3. **Exploitation** : tentative d'exploitation des vulnérabilités identifiées\n   → *Outils : Metasploit Framework, Burp Suite, SQLMap*\n\n4. **Post-exploitation** : maintien de l'accès, élévation de privilèges, mouvement latéral\n   → *Outils : Mimikatz (dump credentials), BloodHound (AD), Meterpreter*\n\n5. **Rapport** : documentation des vulnérabilités, preuves, recommandations priorisées\n   → *Format : risque Critique/Haut/Moyen/Faible, CVSS score, plan de remédiation*` },
    ],
  },

  {
    id: "e6-2019",
    year: 2019,
    epreuve: "E6",
    option: "Commun",
    title: "E6 2019 — Cybersécurité des services informatiques",
    themes: ["Cryptographie", "PKI", "HTTPS", "Sécurité email (SPF/DKIM)"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "e6-2019-q1", numero: "Question 1", intitule: "Expliquez le fonctionnement du chiffrement asymétrique RSA et comment il est utilisé dans l'établissement d'une session TLS 1.3 (TLS Handshake). Quels algorithmes sont utilisés pour l'échange de clés, l'authentification et le chiffrement des données ?", bareme: 7,
        correction: `**RSA (chiffrement asymétrique) :**\n- Paire de clés : publique (chiffrement/vérification) + privée (déchiffrement/signature)\n- Ce qui est chiffré avec la clé publique ne peut être déchiffré qu'avec la clé privée\n\n**TLS 1.3 Handshake (simplifié) :**\n1. **ClientHello** : client envoie versions TLS supportées + cipher suites + clé publique ECDHE\n2. **ServerHello** : serveur choisit cipher suite + envoie son certificat + sa clé publique ECDHE\n3. **Dérivation du secret** : ECDHE (Elliptic Curve Diffie-Hellman Ephemeral) → secret partagé\n4. **Finished** : les deux parties s'authentifient mutuellement via HMAC\n5. **Application data** : chiffrement symétrique AES-256-GCM\n\n**Algorithmes TLS 1.3 :**\n- Échange de clés : **ECDHE** (Perfect Forward Secrecy)\n- Authentification serveur : **RSA** ou **ECDSA** (signature du certificat)\n- Chiffrement données : **AES-256-GCM** ou **ChaCha20-Poly1305**\n- MAC : **SHA-384**` },
    ],
  },

  // ─── TRONC COMMUN ─────────────────────────────────────────────────────────

  {
    id: "tc-maths-2024",
    year: 2024,
    epreuve: "TC",
    option: "Commun",
    title: "Mathématiques 2024 — Cryptographie et graphes",
    themes: ["Cryptographie RSA", "Arithmétique modulaire", "Graphes orientés", "Probabilités"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "tc-maths-2024-q1", numero: "Exercice 1", intitule: "Dans le chiffrement RSA, on choisit p=11 et q=13. (a) Calculez n=p×q et φ(n)=(p-1)(q-1). (b) Choisissez e=7. Vérifiez que pgcd(e,φ(n))=1. (c) Calculez d tel que e×d ≡ 1 [φ(n)] avec l'algorithme d'Euclide étendu.", bareme: 6,
        correction: `**(a)**\nn = 11 × 13 = **143**\nφ(n) = (11-1)(13-1) = 10 × 12 = **120**\n\n**(b)** pgcd(7, 120) :\n120 = 17 × 7 + 1\n7 = 7 × 1 + 0\n→ pgcd(7, 120) = **1** ✓ (7 et 120 sont premiers entre eux)\n\n**(c)** Euclide étendu pour 7d ≡ 1 [120] :\nRemonter l'algorithme : 1 = 120 - 17 × 7\nDonc **d = -17 ≡ 103 [120]**\n\nVérification : 7 × 103 = 721 = 6 × 120 + 1 ✓\n\nClé publique : (e=7, n=143) | Clé privée : (d=103, n=143)` },
      { id: "tc-maths-2024-q2", numero: "Exercice 2", intitule: "Un réseau informatique est modélisé par un graphe orienté G avec 5 sommets (S1 à S5). La matrice d'adjacence est donnée. (a) Représentez le graphe. (b) Déterminez s'il existe un chemin de S1 à S5. (c) Calculez la fermeture transitive.", bareme: 5,
        correction: `**Méthode de résolution :**\n\n**(a)** Tracer le graphe : flèche de Si vers Sj si la matrice[i][j] = 1\n\n**(b)** Existence d'un chemin S1→S5 :\n- Méthode BFS/DFS depuis S1\n- Ou calculer la puissance de la matrice A + A² + A³ + A⁴\n- Un chemin existe si l'entrée [1][5] est non nulle dans la somme\n\n**(c)** Fermeture transitive (algorithme de Roy-Warshall) :\n\`\`\`\nPour k de 1 à n:\n  Pour i de 1 à n:\n    Pour j de 1 à n:\n      T[i][j] = T[i][j] OR (T[i][k] AND T[k][j])\n\`\`\`\n\nLa fermeture transitive T[i][j]=1 signifie qu'il existe un chemin de Si à Sj (quelle que soit sa longueur).` },
    ],
  },

  {
    id: "tc-maths-2023",
    year: 2023,
    epreuve: "TC",
    option: "Commun",
    title: "Mathématiques 2023 — Algorithmes et suites",
    themes: ["Algorithmes de tri", "Complexité", "Suites récurrentes", "Statistiques"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "tc-maths-2023-q1", numero: "Exercice 1", intitule: "On considère l'algorithme de tri par insertion. (a) Tracez son exécution sur le tableau [5, 2, 8, 1, 9, 3]. (b) Quelle est sa complexité temporelle dans le meilleur cas et dans le pire cas ? Justifiez.", bareme: 5,
        correction: `**(a) Exécution :**\n\nÉtape initiale : [**5**, 2, 8, 1, 9, 3]\ni=1 : insert 2 → [**2, 5**, 8, 1, 9, 3]\ni=2 : insert 8 → [2, 5, **8**, 1, 9, 3]\ni=3 : insert 1 → [**1**, 2, 5, 8, 9, 3]\ni=4 : insert 9 → [1, 2, 5, 8, **9**, 3]\ni=5 : insert 3 → [1, 2, **3**, 5, 8, 9]\n\n**(b) Complexité :**\n- **Meilleur cas** : tableau déjà trié → O(n) (1 comparaison par élément)\n- **Pire cas** : tableau trié en ordre inverse → O(n²) (n comparaisons par élément)\n- **Cas moyen** : O(n²)\n\n**Avantage :** efficace pour les petits tableaux et presque triés.` },
    ],
  },

  {
    id: "tc-anglais-2024",
    year: 2024,
    epreuve: "TC",
    option: "Commun",
    title: "Anglais Technique 2024 — Cloud Computing & Cybersecurity",
    themes: ["Cloud computing", "Cybersecurity threats", "Technical vocabulary", "Professional communication"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "tc-anglais-2024-q1", numero: "Question 1 — Compréhension", intitule: "Based on the document about cloud security challenges, answer these questions in English: (a) What are the three main cloud deployment models? (b) What is the 'shared responsibility model'? (c) List 3 cloud-specific threats mentioned in the text.", bareme: 6,
        correction: `**Éléments de réponse attendus :**\n\n**(a) Three cloud deployment models:**\n- **Public cloud**: resources shared among multiple organizations (AWS, Azure, GCP)\n- **Private cloud**: dedicated to a single organization\n- **Hybrid cloud**: combination of public and private clouds\n\n**(b) Shared responsibility model:**\nThe cloud provider is responsible for the security OF the cloud (infrastructure, hardware, software), while the customer is responsible for security IN the cloud (data, access management, applications).\n\n**(c) Cloud-specific threats:**\n- Data breaches due to misconfigured storage buckets\n- Insecure APIs and interfaces\n- Account hijacking through credential theft\n- Insider threats from privileged cloud administrators\n- Denial-of-service attacks targeting cloud resources` },
      { id: "tc-anglais-2024-q2", numero: "Question 2 — Expression", intitule: "Write a professional email (150-200 words) to your IT manager explaining why the company should migrate its file server to a cloud solution. Mention at least 3 benefits and 2 risks with mitigation measures.", bareme: 6,
        correction: `**Structure attendue :**\n\n**Subject:** Cloud Migration Proposal for File Server\n\n**Introduction:** State purpose clearly\n> I am writing to recommend migrating our file server to a cloud-based solution...\n\n**Benefits (3 minimum):**\n- Cost reduction (no hardware maintenance)\n- Scalability (adjust storage on demand)\n- Remote access for distributed teams\n- Automatic backups and disaster recovery\n\n**Risks + mitigation:**\n- Data security → encryption at rest and in transit, MFA\n- Internet dependency → redundant connections, local cache\n- Vendor lock-in → multi-cloud strategy or open standards\n\n**Conclusion:** Call to action\n> I would be happy to discuss this proposal...\n\n**Critères de notation :** vocabulaire technique approprié, structure email professionnelle, argumentation équilibrée, grammaire correcte.` },
    ],
  },

  {
    id: "tc-cge-2024",
    year: 2024,
    epreuve: "TC",
    option: "Commun",
    title: "Culture Générale et Expression 2024",
    themes: ["Synthèse de documents", "Numérique et société", "Expression personnelle", "Argumentation"],
    pdfUrl: "https://www.eduscol.education.fr/sti/si-ens-lgt/ressources_pedagogiques/bts-sio-annales",
    questions: [
      { id: "tc-cge-2024-q1", numero: "Partie 1 — Synthèse", intitule: "À partir du corpus de documents fourni sur le thème 'Intelligence artificielle et emploi', rédigez une synthèse objective de 200-250 mots. Vous dégagerez les idées essentielles sans exprimer votre opinion personnelle.", bareme: 20,
        correction: `**Méthode de la synthèse BTS :**\n\n**1. Analyse du corpus :**\nIdentifier pour chaque document : nature, auteur, date, thèse principale\n\n**2. Plan de la synthèse (3 parties) :**\nI. L'IA comme facteur de transformation du marché du travail\nII. Les secteurs et métiers impactés (suppression / création)\nIII. Les enjeux de formation et d'adaptation\n\n**3. Structure type :**\n- **Introduction** : présentation du thème + annonce du plan (2-3 lignes)\n- **Développement** : 3 paragraphes thématiques avec références aux documents\n- **Conclusion** : synthèse des idées sans avis personnel\n\n**Points de vigilance :**\n- Ne pas juxtaposer les documents : confronter les points de vue\n- Reformuler (pas de citation directe extensive)\n- Équilibre entre les documents\n- Pas de 'je', pas d'opinion\n- Vérifier la cohérence et les transitions\n\n> **Barème indicatif :** Compréhension des documents (8pts) + Organisation/structure (6pts) + Expression écrite (6pts)` },
      { id: "tc-cge-2024-q2", numero: "Partie 2 — Expression personnelle", intitule: "Dans quelle mesure les professionnels de l'informatique sont-ils particulièrement concernés par les transformations induites par l'intelligence artificielle ? Vous développerez votre point de vue argumenté en 200-250 mots.", bareme: 20,
        correction: `**Structure attendue :**\n\n**Introduction :** Accroche + problématique + annonce de plan\n*Ex : "L'intelligence artificielle bouleverse tous les secteurs professionnels. Les informaticiens, à la fois créateurs et utilisateurs de ces technologies, occupent une position singulière..."*\n\n**I. Les informaticiens, acteurs directs de l'IA**\n- Développeurs : nouveaux outils (GitHub Copilot, ChatGPT API)\n- Administrateurs systèmes : automatisation, DevOps\n- Cybersécurité : IA offensives et défensives\n\n**II. Des métiers en mutation profonde**\n- Certaines tâches répétitives automatisées (tests, documentation)\n- Montée en compétence vers la supervision et l'éthique IA\n- Nouveaux métiers : Data Scientist, MLOps, AI Security Engineer\n\n**Conclusion :** Dépassement du constat → perspective\n*"Plutôt que de subir, les professionnels de l'IT ont l'opportunité de piloter ces transformations..."*\n\n**Critères :** Prise de position claire + arguments étayés + exemples concrets + expression soignée` },
    ],
  },
];

export function getAnnaleById(id: string): Annale | undefined {
  return annales.find((a) => a.id === id);
}

export function getAnnalesByFilter(opts: {
  year?: number;
  epreuve?: string;
  option?: string;
}): Annale[] {
  return annales.filter((a) => {
    if (opts.year && a.year !== opts.year) return false;
    if (opts.epreuve && a.epreuve !== opts.epreuve) return false;
    if (opts.option && opts.option !== "Tous" && a.option !== opts.option) return false;
    return true;
  });
}
```

- [ ] **Vérifier TypeScript :**
```bash
cd /Users/sdjbrl/projects/btssio-dev && npx tsc --noEmit 2>&1 | head -20
```
Expected: 0 errors

- [ ] **Commit :**
```bash
git add lib/annales-data.ts
git commit -m "feat: add annales data (E4/E5 SISR/E5 SLAM/E6/TC — 2019-2024)

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 2 — Composant `AnnaleCard`

**Files:**
- Create: `components/AnnaleCard.tsx`

- [ ] **Créer `components/AnnaleCard.tsx` :**

```tsx
import Link from "next/link";
import { FileText, ExternalLink } from "lucide-react";
import type { Annale } from "@/lib/annales-data";

const EPREUVE_COLORS: Record<string, string> = {
  E4: "bg-blue-900/50 text-blue-300 border-blue-700",
  E5: "bg-green-900/50 text-green-300 border-green-700",
  E6: "bg-red-900/50 text-red-300 border-red-700",
  TC: "bg-yellow-900/50 text-yellow-300 border-yellow-700",
};

const OPTION_COLORS: Record<string, string> = {
  SISR: "bg-emerald-900/40 text-emerald-300",
  SLAM: "bg-purple-900/40 text-purple-300",
  Commun: "bg-gray-800 text-gray-400",
};

interface Props {
  annale: Annale;
}

export default function AnnaleCard({ annale }: Props) {
  return (
    <div className="bg-[#1E293B] border border-[#334155] rounded-xl p-5 flex flex-col gap-4 hover:border-[#475569] transition-colors">
      {/* Header */}
      <div className="flex items-start justify-between gap-2">
        <div className="flex gap-2 flex-wrap">
          <span className={`text-xs font-mono font-bold px-2 py-0.5 rounded border ${EPREUVE_COLORS[annale.epreuve] ?? "bg-gray-800 text-gray-300"}`}>
            {annale.epreuve}
          </span>
          <span className={`text-xs px-2 py-0.5 rounded ${OPTION_COLORS[annale.option]}`}>
            {annale.option}
          </span>
        </div>
        <span className="text-2xl font-bold font-mono text-white shrink-0">{annale.year}</span>
      </div>

      {/* Title */}
      <h3 className="text-sm font-medium text-[#CBD5E1] leading-snug">{annale.title}</h3>

      {/* Themes */}
      <div className="flex flex-wrap gap-1.5">
        {annale.themes.slice(0, 3).map((t) => (
          <span key={t} className="text-xs bg-[#0F172A] text-[#64748B] px-2 py-0.5 rounded">
            {t}
          </span>
        ))}
        {annale.themes.length > 3 && (
          <span className="text-xs text-[#475569]">+{annale.themes.length - 3}</span>
        )}
      </div>

      {/* Actions */}
      <div className="flex gap-2 mt-auto pt-2 border-t border-[#334155]">
        <a
          href={annale.pdfUrl}
          target="_blank"
          rel="noopener noreferrer"
          className="flex items-center gap-1.5 px-3 py-1.5 text-xs bg-[#0F172A] hover:bg-[#1E293B] border border-[#334155] hover:border-[#475569] text-[#94A3B8] rounded-lg transition-colors"
        >
          <ExternalLink className="w-3 h-3" />
          PDF officiel
        </a>
        <Link
          href={`/annales/${annale.id}`}
          className="flex items-center gap-1.5 px-3 py-1.5 text-xs bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors ml-auto"
        >
          <FileText className="w-3 h-3" />
          Réviser →
        </Link>
      </div>
    </div>
  );
}
```

- [ ] **Commit :**
```bash
git add components/AnnaleCard.tsx
git commit -m "feat: add AnnaleCard component

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 3 — Composant `AnnaleFilter`

**Files:**
- Create: `components/AnnaleFilter.tsx`

- [ ] **Créer `components/AnnaleFilter.tsx` :**

```tsx
"use client";

interface Filters {
  year: string;
  epreuve: string;
  option: string;
}

interface Props {
  filters: Filters;
  onChange: (filters: Filters) => void;
  totalCount: number;
}

const YEARS = ["Toutes", "2024", "2023", "2022", "2021", "2020", "2019"];
const EPREUVES = ["Toutes", "E4", "E5", "E6", "TC"];
const OPTIONS = ["Tous", "SISR", "SLAM", "Commun"];

export default function AnnaleFilter({ filters, onChange, totalCount }: Props) {
  const select = (field: keyof Filters, value: string) =>
    onChange({ ...filters, [field]: value });

  return (
    <div className="bg-[#1E293B] border border-[#334155] rounded-xl p-4 mb-8">
      <div className="flex flex-wrap gap-4 items-end">
        <FilterGroup
          label="Année"
          values={YEARS}
          current={filters.year}
          onChange={(v) => select("year", v)}
        />
        <FilterGroup
          label="Épreuve"
          values={EPREUVES}
          current={filters.epreuve}
          onChange={(v) => select("epreuve", v)}
        />
        <FilterGroup
          label="Option"
          values={OPTIONS}
          current={filters.option}
          onChange={(v) => select("option", v)}
        />
        <span className="text-sm text-[#64748B] ml-auto self-center">
          {totalCount} sujet{totalCount > 1 ? "s" : ""}
        </span>
      </div>
    </div>
  );
}

function FilterGroup({
  label,
  values,
  current,
  onChange,
}: {
  label: string;
  values: string[];
  current: string;
  onChange: (v: string) => void;
}) {
  return (
    <div className="flex flex-col gap-1.5">
      <span className="text-xs text-[#64748B] font-medium">{label}</span>
      <div className="flex flex-wrap gap-1.5">
        {values.map((v) => (
          <button
            key={v}
            onClick={() => onChange(v)}
            className={`px-3 py-1 text-xs rounded-lg border transition-colors ${
              current === v
                ? "bg-blue-600 border-blue-500 text-white"
                : "bg-[#0F172A] border-[#334155] text-[#94A3B8] hover:border-[#475569]"
            }`}
          >
            {v}
          </button>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Commit :**
```bash
git add components/AnnaleFilter.tsx
git commit -m "feat: add AnnaleFilter component

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 4 — Page `/annales`

**Files:**
- Create: `app/annales/page.tsx`

- [ ] **Créer `app/annales/page.tsx` :**

```tsx
"use client";

import { useState, useMemo } from "react";
import { annales } from "@/lib/annales-data";
import AnnaleCard from "@/components/AnnaleCard";
import AnnaleFilter from "@/components/AnnaleFilter";
import { BookMarked } from "lucide-react";

interface Filters {
  year: string;
  epreuve: string;
  option: string;
}

const DEFAULT_FILTERS: Filters = { year: "Toutes", epreuve: "Toutes", option: "Tous" };

export default function AnnalesPage() {
  const [filters, setFilters] = useState<Filters>(DEFAULT_FILTERS);

  const filtered = useMemo(() => {
    return annales.filter((a) => {
      if (filters.year !== "Toutes" && a.year !== parseInt(filters.year)) return false;
      if (filters.epreuve !== "Toutes" && a.epreuve !== filters.epreuve) return false;
      if (filters.option !== "Tous" && a.option !== filters.option) return false;
      return true;
    });
  }, [filters]);

  return (
    <div className="min-h-screen bg-[#0F172A] text-white">
      <div className="max-w-6xl mx-auto px-4 py-16">
        {/* Header */}
        <div className="mb-10">
          <div className="flex items-center gap-3 mb-4">
            <BookMarked className="w-8 h-8 text-blue-400" />
            <h1 className="text-4xl font-bold font-mono">Annales BTS SIO</h1>
          </div>
          <p className="text-[#94A3B8] max-w-2xl">
            Anciens sujets d&apos;examen 2019–2024 pour toutes les épreuves (E4, E5, E6) et le tronc commun.
            Questions reformulées + corrections indicatives non officielles.
          </p>
        </div>

        {/* Filters */}
        <AnnaleFilter filters={filters} onChange={setFilters} totalCount={filtered.length} />

        {/* Grid */}
        {filtered.length === 0 ? (
          <div className="text-center py-16 text-[#475569]">
            <BookMarked className="w-12 h-12 mx-auto mb-4 opacity-30" />
            <p>Aucun sujet trouvé pour ces filtres.</p>
          </div>
        ) : (
          <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
            {filtered.map((a) => (
              <AnnaleCard key={a.id} annale={a} />
            ))}
          </div>
        )}

        {/* Disclaimer */}
        <p className="mt-12 text-xs text-[#475569] text-center border-t border-[#1E293B] pt-6">
          ⚠️ Les questions sont reformulées à des fins pédagogiques. Les corrections sont indicatives et non officielles.
          Liens PDF vers les sources officielles Eduscol.
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Vérifier compilation :**
```bash
npx tsc --noEmit 2>&1 | head -10
npm run lint 2>&1 | tail -5
```
Expected: 0 errors

- [ ] **Commit :**
```bash
git add app/annales/page.tsx
git commit -m "feat: add /annales list page with filters

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 5 — Page `/annales/[id]`

**Files:**
- Create: `app/annales/[id]/page.tsx`

- [ ] **Créer le répertoire et le fichier :**

```bash
mkdir -p /Users/sdjbrl/projects/btssio-dev/app/annales/\[id\]
```

- [ ] **Créer `app/annales/[id]/page.tsx` :**

```tsx
"use client";

import { useState } from "react";
import { notFound, useParams } from "next/navigation";
import Link from "next/link";
import { getAnnaleById } from "@/lib/annales-data";
import { ChevronDown, ChevronRight, ExternalLink, ArrowLeft } from "lucide-react";
import ReactMarkdown from "react-markdown";

export default function AnnaleDetailPage() {
  const { id } = useParams<{ id: string }>();
  const annale = getAnnaleById(id);

  if (!annale) notFound();

  return (
    <div className="min-h-screen bg-[#0F172A] text-white">
      <div className="max-w-4xl mx-auto px-4 py-12">
        {/* Breadcrumb */}
        <Link
          href="/annales"
          className="inline-flex items-center gap-2 text-sm text-[#64748B] hover:text-white transition-colors mb-8"
        >
          <ArrowLeft className="w-4 h-4" />
          Retour aux annales
        </Link>

        {/* Header */}
        <div className="mb-8">
          <div className="flex flex-wrap gap-2 mb-3">
            <span className="text-xs font-mono font-bold px-2 py-0.5 rounded border bg-blue-900/50 text-blue-300 border-blue-700">
              {annale.epreuve}
            </span>
            <span className="text-xs px-2 py-0.5 rounded bg-gray-800 text-gray-400">
              {annale.option}
            </span>
            <span className="text-xs px-2 py-0.5 rounded bg-[#1E293B] text-[#94A3B8]">
              {annale.year}
            </span>
          </div>
          <h1 className="text-3xl font-bold font-mono mb-4">{annale.title}</h1>

          {/* Themes */}
          <div className="flex flex-wrap gap-2 mb-6">
            {annale.themes.map((t) => (
              <span key={t} className="text-xs bg-[#1E293B] text-[#94A3B8] border border-[#334155] px-2 py-1 rounded">
                {t}
              </span>
            ))}
          </div>

          <a
            href={annale.pdfUrl}
            target="_blank"
            rel="noopener noreferrer"
            className="inline-flex items-center gap-2 px-4 py-2 bg-[#1E293B] border border-[#334155] hover:border-[#475569] text-sm text-[#94A3B8] rounded-lg transition-colors"
          >
            <ExternalLink className="w-4 h-4" />
            Voir le sujet officiel (PDF Eduscol)
          </a>
        </div>

        {/* Questions */}
        <div className="space-y-4">
          <h2 className="text-xl font-semibold font-mono mb-6 text-[#CBD5E1]">
            Questions et corrections
          </h2>
          {annale.questions.map((q) => (
            <QuestionAccordion key={q.id} question={q} />
          ))}
        </div>

        <p className="mt-10 text-xs text-[#475569] border-t border-[#1E293B] pt-6">
          ⚠️ Corrigé indicatif non officiel — reformulé à des fins pédagogiques.
        </p>
      </div>
    </div>
  );
}

function QuestionAccordion({
  question,
}: {
  question: { id: string; numero: string; intitule: string; bareme: number; correction: string };
}) {
  const [open, setOpen] = useState(false);

  return (
    <div className="border border-[#334155] rounded-xl overflow-hidden">
      {/* Question header */}
      <div className="bg-[#1E293B] p-5">
        <div className="flex items-start justify-between gap-3 mb-2">
          <span className="text-xs font-mono text-blue-400 font-bold">{question.numero}</span>
          <span className="text-xs text-[#64748B] shrink-0">{question.bareme} pts</span>
        </div>
        <p className="text-sm text-[#CBD5E1] leading-relaxed">{question.intitule}</p>
      </div>

      {/* Correction toggle */}
      <button
        onClick={() => setOpen(!open)}
        className="w-full flex items-center justify-between px-5 py-3 bg-[#0F172A] border-t border-[#334155] text-sm text-[#64748B] hover:text-white hover:bg-[#1E293B] transition-colors"
        aria-expanded={open}
      >
        <span className="flex items-center gap-2">
          {open ? <ChevronDown className="w-4 h-4" /> : <ChevronRight className="w-4 h-4" />}
          {open ? "Masquer la correction" : "Voir la correction indicative"}
        </span>
      </button>

      {open && (
        <div className="p-5 bg-[#0F172A] border-t border-[#334155] prose prose-invert prose-sm max-w-none
          prose-code:bg-[#1E293B] prose-code:px-1 prose-code:rounded prose-code:text-green-300
          prose-pre:bg-[#1E293B] prose-pre:border prose-pre:border-[#334155]">
          <ReactMarkdown>{question.correction}</ReactMarkdown>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Installer react-markdown si nécessaire :**
```bash
cd /Users/sdjbrl/projects/btssio-dev && npm list react-markdown 2>/dev/null || npm install react-markdown
```

- [ ] **Vérifier compilation :**
```bash
npx tsc --noEmit 2>&1 | head -10
npm run lint 2>&1 | tail -5
```

- [ ] **Commit :**
```bash
git add app/annales/
git commit -m "feat: add /annales/[id] detail page with question accordions

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 6 — Widget annales dans `/examens` + lien Navbar

**Files:**
- Modify: `app/examens/page.tsx`
- Modify: `components/Navbar.tsx`

- [ ] **Modifier `app/examens/page.tsx` — ajouter l'import et le widget à la fin du JSX (avant la balise de fermeture du div principal) :**

Lire le fichier pour trouver le bon endroit, puis ajouter juste avant la dernière balise `</div>` principale :

```tsx
// Ajout en haut du fichier avec les autres imports :
import Link from "next/link";
import { annales } from "@/lib/annales-data";
import AnnaleCard from "@/components/AnnaleCard";

// Dans le JSX, ajouter à la fin (avant dernier </div>) :
{/* Annales récentes */}
<section className="mt-16 border-t border-[#334155] pt-12">
  <div className="flex items-center justify-between mb-6">
    <h2 className="text-xl font-semibold font-mono">📋 Annales récentes</h2>
    <Link
      href="/annales"
      className="text-sm text-blue-400 hover:text-blue-300 transition-colors"
    >
      Voir toutes les annales →
    </Link>
  </div>
  <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
    {annales
      .sort((a, b) => b.year - a.year)
      .slice(0, 6)
      .map((a) => (
        <AnnaleCard key={a.id} annale={a} />
      ))}
  </div>
</section>
```

- [ ] **Modifier `components/Navbar.tsx` — ajouter le lien Annales après Examens :**

```tsx
// Dans le tableau navLinks, après { label: "Examens", href: "/examens" } :
{ label: "Annales", href: "/annales" },
```

- [ ] **Vérifier compilation :**
```bash
npx tsc --noEmit 2>&1 | head -10
npm run lint 2>&1 | tail -5
```

- [ ] **Commit final :**
```bash
git add app/examens/page.tsx components/Navbar.tsx
git commit -m "feat: add annales widget to /examens and Navbar link

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
```

---

## Task 7 — Push et vérification production

- [ ] **Push :**
```bash
git push origin main
```

- [ ] **Attendre le déploiement Vercel (~60s), puis vérifier :**
```bash
sleep 90
curl -s -o /dev/null -w "%{http_code}" https://btssio.saiddev.fr/annales
# Expected: 307 (redirect to login — normal, site is private)

curl -s -o /dev/null -w "%{http_code}" https://btssio.saiddev.fr/annales/e4-2024
# Expected: 307
```

- [ ] **Vérifier le build Vercel (0 erreurs) :**
```bash
vercel ls 2>&1 | head -5
```

---

## Self-Review

**Spec coverage :**
- ✅ Page `/annales` avec filtres
- ✅ Page `/annales/[id]` avec corrections
- ✅ Widget dans `/examens`
- ✅ Lien Navbar
- ✅ Années 2019–2024
- ✅ E4, E5 SISR, E5 SLAM, E6, TC (Maths, Anglais, CGE)
- ✅ Liens PDF officiels
- ✅ Corrections indicatives

**Types consistent :**
- `Annale` et `AnnaleQuestion` définis en Task 1, utilisés en Task 2–5
- `getAnnaleById()` et `getAnnalesByFilter()` définis Task 1, `getAnnaleById` utilisé Task 5
- `AnnaleFilter` props (`filters`, `onChange`, `totalCount`) : définis Task 3, passés Task 4

**Placeholders :** aucun TBD/TODO.
